import { ClientPortal } from "@/components/client-portal";

export const metadata = { title: "Área de clientes | IMPULSA FIT", robots: { index: false, follow: false } };

export default function ClientPage() { return <ClientPortal />; }
